/**
 * Self-Test Layer
 * ================
 * Menu-driven unit tests (Phases 1-4) that run inside the application
 * without any test framework, so learners can verify every component
 * interactively. Continuous-integration tests live separately under
 * {@code src/test/java}.
 */
package com.supplychain.selftest;
