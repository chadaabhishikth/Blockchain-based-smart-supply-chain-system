/**
 * Domain Layer
 * =============
 * The heart of the system: the blockchain data structures (merkle,
 * ledger), the typed domain model, the supply chain business service
 * and its typed results (dto). Pure business logic — no console I/O,
 * no persistence, no framework.
 */
package com.supplychain.domain;
