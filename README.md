# Blockchain-based-smart-supply-chain-system
You are building an immutable (unchangeable) history tracker for physical products. When a factory produces an item, a digital record is created. Every time that item changes hands (factory $\rightarrow$ distributor $\rightarrow$ retailer $\rightarrow$ consumer), a new transaction is recorded.
